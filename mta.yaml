_schema-version: 3.3.0
ID: school-capm
description: A simple CAP project.
version: 1.0.0
modules:
- name: school-capm-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: school-capm-hdi
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    disk-quota: 512M
    instances: 1
    memory: 256M
  build-parameters:
    builder: npm-ci
- name: school-capm-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: school-capm-hdi
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: school-capm-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: school-capm-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - project1.zip
      name: project1
      target-path: resources/
- name: project1
  type: html5
  path: app/project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: school-capm-hdi
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: school-capm-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: school-capm-html5-service
    service-plan: app-host
- name: school-capm-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: school-capm-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install --legacy-peer-deps
    - npx cds build --production
